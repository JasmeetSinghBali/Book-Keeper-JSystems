import { useSession } from 'next-auth/react';
import create from 'zustand';
import { trpcClient } from '../utils/Clientrpc';


// interface SampleInterface {
// }

// export const useCurrentUserInfo = create<SampleInterface>((set): any => ({
    
// }));